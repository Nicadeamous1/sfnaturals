import {build} from 'esbuild';import {chromium} from 'playwright';import fs from 'node:fs/promises';import {gzipSync} from 'node:zlib';
await build({entryPoints:['assets/3d/source/bake-environment.mjs'],bundle:true,format:'esm',outfile:'dist/js/bake-environment.js'});await fs.writeFile('dist/bake-environment.html','<script type="module" src="js/bake-environment.js"></script>');const browser=await chromium.launch({executablePath:process.env.BROWSER_EXECUTABLE,channel:process.env.BROWSER_EXECUTABLE?undefined:(process.env.BROWSER_CHANNEL||'msedge'),headless:true});const page=await browser.newPage();await page.goto('http://127.0.0.1:4188/bake-environment.html');await page.waitForFunction(()=>!!window.bakedEnvironment);const {width,height,data}=await page.evaluate(()=>window.bakedEnvironment);await fs.writeFile('dist/assets/3d/studio-environment.v1.bin.gz',gzipSync(Buffer.from(new Uint16Array(data).buffer)));await fs.writeFile('dist/assets/3d/studio-environment.v1.json',JSON.stringify({width,height,format:'RGBA',type:'HalfFloat',mapping:'CubeUVReflection',colorSpace:'LinearSRGB',source:'Three.js RoomEnvironment, prefiltered offline at size 64'}));console.log({width,height,bytes:(await fs.stat('dist/assets/3d/studio-environment.v1.bin.gz')).size});await browser.close();await fs.unlink('dist/bake-environment.html');await fs.unlink('dist/js/bake-environment.js');


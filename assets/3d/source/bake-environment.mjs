import * as THREE from 'three';import {RoomEnvironment} from 'three/addons/environments/RoomEnvironment.js';
const renderer=new THREE.WebGLRenderer();const generator=new THREE.PMREMGenerator(renderer);const room=new RoomEnvironment();const target=generator.fromScene(room,.04,.1,100,{size:64});const data=new Uint16Array(target.width*target.height*4);renderer.readRenderTargetPixels(target,0,0,target.width,target.height,data);window.bakedEnvironment={width:target.width,height:target.height,data:Array.from(data)};target.dispose();room.dispose();generator.dispose();renderer.dispose();
